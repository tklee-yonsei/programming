#include <stdio.h>
#include <string.h>

enum Color {
  RED = 1,
  GREEN,
  BLUE
};  // RED는 1, GREEN은 2, BLUE는 3
typedef enum {
  MON, TUE,
  WED, THU,
  FRI, SAT, SUN
} Weekday;

int main() {


  return 0;
}
