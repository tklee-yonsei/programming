#include <stdio.h>

int main() {
    int var_floor = 3; // 층 수를 입력합니다.
    int var_ho = 4; // 호 수를 제공합니다.
    
    // 층 수가 홀수이고, 호 수가 홀수이면, ev1을 사용합니다.
    // 층 수가 홀수이고, 호 수가 짝수이면, ev2을 사용합니다.
    // 층 수가 짝수이고, 호 수가 홀수이면, ev3을 사용합니다.
    // 층 수가 짝수이고, 호 수가 짝수이면, ev4을 사용합니다.

    return 0;
}
