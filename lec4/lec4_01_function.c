#include <stdio.h>

double get_average(double left, double right) {
    return (left + right) / 2;
    // double result = (left + right);
    // result /= 2;
    // return result;
}

int main() {
    return 0;
}
