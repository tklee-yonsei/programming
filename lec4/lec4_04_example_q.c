#include <stdio.h>

/**
 * @brief countdown x to 0
 * This ends when x is less than 0.
 * 
 * @param x countdown starting point
 */
void countdown(int x);

int main() {
    countdown(10);
    
    printf("----\n");
    countdown(4);

    return 0;
}

void countdown(int x) {

}